<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Unit-test project: build as a plain net11.0 library (NOT a MAUI/iOS app bundle).
         Core/Maui.UI/Spectrogram now expose a net11.0 base target so this reference resolves.
         Test contents (assertions, cross-platform paths) are Phase 15 work. -->
    <TargetFrameworks></TargetFrameworks>
    <TargetFramework>net11.0</TargetFramework>
    <UseMaui>false</UseMaui>
    <IsPackable>false</IsPackable>
    <!-- Consume types from Core/Maui.UI, which are compiled with EnablePreviewFeatures=true. -->
    <EnablePreviewFeatures>true</EnablePreviewFeatures>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.0.1" />
    <PackageReference Include="Moq" Version="4.20.72" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="coverlet.collector" Version="6.0.4">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SimTuning.Core\SimTuning.Core.csproj" />
    <ProjectReference Include="..\SimTuning.Maui.UI\SimTuning.Maui.UI.csproj" />
  </ItemGroup>
</Project>
